name: Lab2b CI

on:
  push:
    paths:
      - "lab2b/**"
      - ".github/workflows/lab2b-ci.yml"
  pull_request:
    paths:
      - "lab2b/**"
      - ".github/workflows/lab2b-ci.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
      - name: Build (Maven)
        run: mvn -f lab2b/pom.xml -B clean package

